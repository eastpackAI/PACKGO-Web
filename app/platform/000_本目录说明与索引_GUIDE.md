# platform 路由目录说明与索引

本目录负责 `/platform`（平台能力 / 为什么选择 PACKGO）独立页面的 URL 与页面元数据。

- 文案只来自 `config/standardHome.ts` 的 `platform`。
- 正文由 `components/standard-view/StandardDetailView.tsx` 按路径渲染。
- 不新增未经确认的能力承诺；模块内容保持与首页一致。

